import AppKit
import SiftCore

// Placeholder entry point. The real LSUIElement SwiftUI app lands in a
// later step (TaskList #10) — keeping the executable target compilable
// for now so the workspace builds end-to-end.
let app = NSApplication.shared
app.setActivationPolicy(.accessory)
print("Sift menu bar app — not yet implemented. Use the `sift` CLI for now.")
